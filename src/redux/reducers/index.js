import {
    SIGNUP_FETCHING,
    SIGNUP_SUCCESS,
    SIGNUP_FAILURE
} from '../actions'

export const initialState = {}



export const reducer = (state = initialState, action) =>{

    switch(action.type){



        default: return state;
    }
} 